const doc = {
  // ── Doc chrome ────────────────────────────────────────────────────────
  "doc.field": "Field",
  "doc.type": "Type",
  "doc.description": "Description",
  "doc.anchorCopied": "Anchor link copied",
  "doc.anchorNavigated": "Jumped to section",
  "doc.copyAnchor": "Copy anchor link",
  "doc.copyCurl": "Copy curl",
  "doc.title": "OpenAPI Docs",

  // ── Authentication ────────────────────────────────────────────────────
  "doc.authentication.title": "Authentication",
  "doc.authentication.token.title": "Access token",
  "doc.authentication.token.p1": "Calls to `/openapi/**` must carry a user access token in the request header:",
  "doc.authentication.token.p2": "Access tokens are generated or reset on the `Settings → Profile` page. A user holds at most one access token at a time; resetting invalidates the old one immediately.",
  "doc.authentication.token.p3": "Tokens never expire, and they carry the user's role: calling an endpoint over OpenAPI grants exactly the permissions that user would have calling the matching `/api/**` endpoint after signing in through the UI.",
  "doc.authentication.limits.title": "Limitations",
  "doc.authentication.limits.p1": "Some destructive operations are marked as closed to OpenAPI and return `405 Method Not Allowed`. Deleting an application, for example, must be done from the Danger Zone in the UI; deleting sandbox instances and sandbox files is part of the sandbox lifecycle and stays callable under `/openapi/sandbox/**`.",
  "doc.authentication.limits.p2": "A missing or malformed `Authorization` header returns `401 Unauthorized`. A well-formed token that no longer exists or has been reset also returns `401`.",
  "doc.authentication.example.title": "Example",
  "doc.authentication.example.p1": "Calling a resource discovery endpoint with curl:",
  "doc.authentication.example.p2": "A typical response:",

  // ── Discovery ─────────────────────────────────────────────────────────
  "doc.discovery.title": "Resource discovery",
  "doc.discovery.overview.title": "Overview",
  "doc.discovery.overview.p1": "Automation scripts usually start by listing the available namespaces, environments and domains before picking a target to act on. The discovery endpoints live at the `/openapi` root and take no path parameters.",
  "doc.discovery.overview.p2": "Environments are redacted on the way out: the Kubernetes API token, image registry password, Git credentials and other sensitive fields all come back as `null`.",
  "doc.discovery.namespaces.title": "List namespaces",
  "doc.discovery.namespaces.summary": "Returns every namespace visible to the current user.",
  "doc.discovery.namespaces.response.title": "Response",
  "doc.discovery.namespaces.response.id": "Namespace NanoId.",
  "doc.discovery.namespaces.response.name": "Namespace name — this is the {namespace} in application URLs.",
  "doc.discovery.namespaces.response.description": "Optional description.",
  "doc.discovery.namespaces.response.createdTime": "Creation time.",
  "doc.discovery.environments.title": "List environments",
  "doc.discovery.environments.summary": "Returns every environment, with sensitive fields redacted.",
  "doc.discovery.environments.response.title": "Response",
  "doc.discovery.environments.response.name": "Environment name — this is the environment in the deployment endpoints.",
  "doc.discovery.environments.response.apiServerUrl": "Kubernetes API server address.",
  "doc.discovery.environments.response.apiServerToken": "Redacted; always null.",
  "doc.discovery.environments.response.workNamespace": "Namespace the build jobs run in.",
  "doc.discovery.environments.response.buildStorageClass": "StorageClass name used by build jobs.",
  "doc.discovery.environments.response.registryUrl": "Image registry address.",
  "doc.discovery.environments.response.registryPassword": "Redacted; always null.",
  "doc.discovery.environments.response.gitCredential": "Always null over OpenAPI, for security reasons.",
  "doc.discovery.environments.example.title": "Example",
  "doc.discovery.domains.title": "List domains",
  "doc.discovery.domains.summary": "Returns every domain the platform manages, including HTTPS and certificate state.",
  "doc.discovery.domains.response.title": "Response",
  "doc.discovery.domains.response.host": "Host name; wildcards (a leading *.) are supported.",
  "doc.discovery.domains.response.https": "Whether HTTPS is enabled.",
  "doc.discovery.domains.response.certMode": "AUTO (issued automatically) or UPLOADED (user-supplied certificate).",
  "doc.discovery.domains.response.hasUploadedCert": "Whether a PEM certificate has been uploaded.",
  "doc.discovery.domains.response.certSubject": "Subject of the uploaded certificate; null when none is uploaded.",
  "doc.discovery.domains.response.certNotAfter": "Certificate expiry (ISO datetime); null when none is uploaded.",

  // ── Applications ──────────────────────────────────────────────────────
  "doc.applications.title": "Applications",
  "doc.applications.overview.title": "Overview",
  "doc.applications.overview.p1": "The application is the core resource in OOPS. Each one belongs to a namespace, can be bound to several environments, and carries its own build config, runtime config and Service/Ingress config per environment. Every endpoint below sits under the path prefix `/openapi/namespaces/{namespace}/applications`, and applications are addressed by `name` rather than a numeric id.",
  "doc.applications.crud.title": "Basic CRUD",
  "doc.applications.crud.list.summary": "Paged keyword search over the applications in a namespace.",
  "doc.applications.crud.list.params.title": "Query parameters",
  "doc.applications.crud.list.params.keyword": "Fuzzy match on the application name; optional.",
  "doc.applications.crud.list.params.page": "Page number, 1-based, defaults to 1.",
  "doc.applications.crud.list.params.size": "Page size, defaults to 10.",
  "doc.applications.crud.list.params.ownerOnly": "Return only the applications the caller owns; defaults to false.",
  "doc.applications.crud.get.summary": "Fetch a single application by name.",
  "doc.applications.crud.create.summary": "Create an application. The response data is the new application's id.",
  "doc.applications.crud.create.body.title": "Request body (ApplicationConfigDto.Profile)",
  "doc.applications.crud.create.body.name": "Application name; unique within the namespace and must be a valid K8s resource name.",
  "doc.applications.crud.create.body.namespace": "Normally matches the namespace in the URL; the backend takes the URL as authoritative.",
  "doc.applications.crud.create.body.owner": "Overwritten with the caller's userId on creation, so it can be omitted.",
  "doc.applications.crud.create.body.collaborators": "List of collaborator userId strings; deduplicated, with the owner removed.",
  "doc.applications.crud.update.summary": "Update the application profile (description, collaborators, and so on).",
  "doc.applications.crud.update.p1": "The request body matches the create call. A delete endpoint exists but is closed to OpenAPI — deletion has to go through the Danger Zone in the UI.",
  "doc.applications.build.title": "Build config",
  "doc.applications.build.get.summary": "Fetch the application's global build config.",
  "doc.applications.build.put.summary": "Update the build config.",
  "doc.applications.build.body.title": "Request body (ApplicationConfigDto.BuildConfig)",
  "doc.applications.build.body.sourceType": "Source type; either GIT or ZIP.",
  "doc.applications.build.body.repository": "Git repository URL; for ZIP, the object storage key template.",
  "doc.applications.build.body.dockerfileType": "Dockerfile type (a built-in enum).",
  "doc.applications.build.body.dockerfilePath": "Path to the Dockerfile relative to the repository root.",
  "doc.applications.build.body.dockerfileContent": "Inline Dockerfile content; mutually exclusive with path.",
  "doc.applications.build.body.buildImage": "Image used for the build step.",
  "doc.applications.build.envGet.summary": "Fetch the per-environment build config overrides.",
  "doc.applications.build.envPut.summary": "Replace the per-environment build configs wholesale (the body is an array).",
  "doc.applications.build.envBody.title": "Request body element (BuildEnvironmentConfig)",
  "doc.applications.build.envBody.buildCommand": "Custom build command for this environment.",
  "doc.applications.runtime.title": "Runtime config",
  "doc.applications.runtime.get.summary": "Fetch the runtime config, health checks included.",
  "doc.applications.runtime.put.summary": "Update the runtime config.",
  "doc.applications.runtime.body.title": "Request body (ApplicationConfigDto.RuntimeSpec)",
  "doc.applications.runtime.body.environmentConfigs": "Array of per-environment resource configs; see RuntimeEnvironmentConfig below for the element shape.",
  "doc.applications.runtime.body.livenessPath": "HTTP liveness probe path, for example /healthz.",
  "doc.applications.runtime.body.readinessPath": "HTTP readiness probe path; drives Service traffic and the rollout completion verdict.",
  "doc.applications.runtime.envConfig.title": "RuntimeEnvironmentConfig",
  "doc.applications.runtime.envConfig.cpuRequest": "K8s resource quantity, for example 100m.",
  "doc.applications.runtime.envConfig.cpuLimit": "For example 500m.",
  "doc.applications.runtime.envConfig.memoryRequest": "For example 128Mi.",
  "doc.applications.runtime.envConfig.memoryLimit": "For example 512Mi.",
  "doc.applications.runtime.envConfig.replicas": "Replica count; defaults to 1.",
  "doc.applications.runtime.envGet.summary": "Fetch just the per-environment runtime configs (without healthCheck).",
  "doc.applications.runtime.envPut.summary": "Replace the per-environment runtime configs wholesale (the body is an array of RuntimeEnvironmentConfig).",
  "doc.applications.environments.title": "Environment bindings",
  "doc.applications.environments.get.summary": "Return the environments the application is bound to.",
  "doc.applications.environments.put.summary": "Replace the application's environment bindings wholesale.",
  "doc.applications.environments.body.title": "Request body element (EnvironmentBinding)",
  "doc.applications.environments.body.environment": "Target environment name; must exist in /openapi/environments.",
  "doc.applications.expert.title": "Expert config",
  "doc.applications.expert.get.summary": "Fetch the advanced deployment config.",
  "doc.applications.expert.put.summary": "Update the advanced deployment config.",
  "doc.applications.expert.body.title": "Request body (ApplicationConfigDto.ExpertConfig)",
  "doc.applications.expert.body.environmentConfigs": "Array of per-environment advanced configs; see ExpertEnvironmentConfig below for the element shape.",
  "doc.applications.expert.body.serviceAccountName": "Kubernetes ServiceAccount name to use when deploying to this environment.",
  "doc.applications.service.title": "Service and domains",
  "doc.applications.service.get.summary": "Fetch the Service/Ingress config.",
  "doc.applications.service.put.summary": "Update the Service/Ingress config.",
  "doc.applications.service.body.title": "Request body (ApplicationConfigDto.ServiceConfig)",
  "doc.applications.service.body.port": "Port the container listens on.",
  "doc.applications.service.body.host": "External host name; must exist in domain management.",
  "doc.applications.service.body.https": "Whether HTTPS is enabled.",
  "doc.applications.service.hostCheck.summary": "Check whether the given host is already taken by another application.",
  "doc.applications.service.clusterDomain.summary": "Return the in-cluster address of the application in the given environment.",
  "doc.applications.status.title": "Status and operations",
  "doc.applications.status.get.summary": "Return the runtime status of every Pod of the application in the given environment.",
  "doc.applications.status.podView.title": "Response array element (ApplicationPodStatusView)",
  "doc.applications.status.podView.name": "Pod name.",
  "doc.applications.status.podView.status": "Running / Pending / Failed, and so on.",
  "doc.applications.status.watch.summary": "Push Pod status changes over Server-Sent Events (the response is not wrapped in Result).",
  "doc.applications.status.currentImage.summary": "Return the image currently running in the given environment.",
  "doc.applications.status.lastPipeline.summary": "Return the application's most recent successful pipeline.",
  "doc.applications.status.resources.summary": "Return the Kubernetes resources the application owns, for the read-only expert view.",
  "doc.applications.status.resourceView.title": "Response array element (ApplicationResourceView)",
  "doc.applications.status.resourceView.kind": "Resource kind, for example StatefulSet / Service / IngressRoute.",
  "doc.applications.status.resourceView.name": "Resource name.",
  "doc.applications.status.resourceView.data": "Resource content as YAML/JSON.",
  "doc.applications.status.restart.summary": "Restart the given Pod. The request body is empty.",

  // ── Pipelines ─────────────────────────────────────────────────────────
  "doc.pipelines.title": "Pipelines",
  "doc.pipelines.overview.title": "Overview",
  "doc.pipelines.overview.p1": "Every deployment creates a Pipeline row in the database backed by one Kubernetes Job: it clones the source, optionally runs a build command, then packages and pushes an image with Buildah. Once the build finishes, `deployMode` decides whether to deploy right away. Pipeline logs are pushed per build step over `SSE`; OpenAPI exposes no log endpoint.",
  "doc.pipelines.overview.p2": "Every endpoint below sits under the path prefix `/openapi/namespaces/{namespace}/applications/{name}/pipelines`, and each pipeline is addressed by its NanoId `id`.",
  "doc.pipelines.list.title": "List pipelines",
  "doc.pipelines.list.summary": "Paged query over an application's pipelines, optionally filtered by environment.",
  "doc.pipelines.list.params.title": "Query parameters",
  "doc.pipelines.list.params.environment": "Optional; filter by environment name.",
  "doc.pipelines.list.params.page": "1-based, defaults to 1.",
  "doc.pipelines.list.params.size": "Defaults to 10.",
  "doc.pipelines.list.item.title": "Page element (PipelineDto)",
  "doc.pipelines.list.item.id": "Pipeline ID.",
  "doc.pipelines.list.item.name": "Pipeline name (the K8s Job name).",
  "doc.pipelines.list.item.status": "Pipeline status enum; see the note below.",
  "doc.pipelines.list.item.artifact": "Build artifact (image tag and so on).",
  "doc.pipelines.list.item.publishType": "Source type, GIT or ZIP.",
  "doc.pipelines.list.item.publishConfig": "Source details, with a type discriminator. GIT: repository, branch; ZIP: objectKey or url.",
  "doc.pipelines.list.item.deployMode": "Either IMMEDIATE or MANUAL.",
  "doc.pipelines.list.item.operatorId": "userId of whoever triggered it.",
  "doc.pipelines.list.item.message": "Failure reason or status note.",
  "doc.pipelines.list.item.triggerType": "Either RELEASE or ROLLBACK.",
  "doc.pipelines.list.item.rollbackFrom": "ID of the pipeline being rolled back to; set on rollback pipelines only.",
  "doc.pipelines.list.item.statuses": "`status` values: `INITIALIZED`, `RUNNING`, `BUILD_SUCCEEDED`, `DEPLOYING`, `ROLLING_OUT`, `SUCCEEDED`, `ERROR`, `STOPPED`.",
  "doc.pipelines.get.title": "Get one pipeline",
  "doc.pipelines.get.summary": "Fetch pipeline details by id.",
  "doc.pipelines.stop.title": "Stop a pipeline",
  "doc.pipelines.stop.summary": "Stop a running pipeline. The request body is empty.",
  "doc.pipelines.stop.p1": "Only valid for pipelines in `RUNNING` / `BUILD_SUCCEEDED` / `DEPLOYING`. Calling it on a finished pipeline, or one already rolling out, returns a business error.",
  "doc.pipelines.deploy.title": "Deploy a built pipeline manually",
  "doc.pipelines.deploy.summary": "Trigger deployment for a successfully built pipeline with deployMode=MANUAL. The request body is empty.",
  "doc.pipelines.deploy.p1": "Only applies to pipelines with `status=BUILD_SUCCEEDED`. `IMMEDIATE` pipelines move into the deploy phase automatically once the build ends, so this call is not needed.",
  "doc.pipelines.rollback.title": "Roll back to an earlier version",
  "doc.pipelines.rollback.summary": "Create a rollback pipeline from a successful one and re-release its artifact.",
  "doc.pipelines.rollback.p1": "A rollback pipeline has `triggerType` `ROLLBACK`: it reuses the historic artifact, skips the build phase, and goes straight to deployment.",

  // ── Deployments ───────────────────────────────────────────────────────
  "doc.deployments.title": "Deployments",
  "doc.deployments.overview.title": "Overview",
  "doc.deployments.overview.p1": "Deployment is how a pipeline gets triggered. The `strategy` field in the request body decides where the build takes its source from: a Git repository or a ZIP archive. With a ZIP source, first call the upload endpoint for a presigned object storage URL, upload the file, then start the deployment.",
  "doc.deployments.overview.p2": "OOPS does not allow concurrent deployments of the same application: if it already has a pipeline in `RUNNING` or `DEPLOYING`, the call is rejected.",
  "doc.deployments.upload.title": "Request a source upload URL (ZIP only)",
  "doc.deployments.upload.summary": "Return a presigned URL for uploading a ZIP source archive; available only when object storage is configured.",
  "doc.deployments.upload.body.title": "Request body (ObjectStorageUploadCommand)",
  "doc.deployments.upload.body.fileName": "Original file name, used to build the object storage key.",
  "doc.deployments.upload.body.fileSize": "File size in bytes. Anything over oops.object-storage.max-file-size is rejected.",
  "doc.deployments.upload.body.contentType": "Content-Type to use for the upload, for example application/zip.",
  "doc.deployments.upload.response.title": "Response (ObjectStorageUploadResult)",
  "doc.deployments.upload.response.objectKey": "Key in object storage; pass it as strategy.objectKey when deploying.",
  "doc.deployments.upload.response.objectUrl": "Final download address of the object (for display only).",
  "doc.deployments.upload.response.uploadUrl": "Presigned PUT URL the client uploads the file to.",
  "doc.deployments.upload.response.headers": "Headers to send with the upload; keys and values are strings.",
  "doc.deployments.upload.example.title": "Upload example",
  "doc.deployments.trigger.title": "Trigger a deployment",
  "doc.deployments.trigger.summary": "Create a pipeline and start building immediately. The response data is the new pipeline's id.",
  "doc.deployments.trigger.body.title": "Request body (DeployCommand)",
  "doc.deployments.trigger.body.environment": "Target environment name; must be one the application is bound to.",
  "doc.deployments.trigger.body.deployMode": "Either IMMEDIATE or MANUAL. Under MANUAL you call the deploy endpoint yourself once the build finishes.",
  "doc.deployments.trigger.body.strategy": "Where the source comes from. Carries a type field; see the two shapes below.",
  "doc.deployments.trigger.git.title": "Git source",
  "doc.deployments.trigger.git.type": "Always GIT.",
  "doc.deployments.trigger.git.branch": "Branch name or commit.",
  "doc.deployments.trigger.zip.title": "ZIP source",
  "doc.deployments.trigger.zip.type": "Always ZIP.",
  "doc.deployments.trigger.zip.objectKey": "Object storage upload: the objectKey returned by the previous step. Mutually exclusive with url.",
  "doc.deployments.trigger.zip.url": "Public address: a directly downloadable ZIP URL. Mutually exclusive with objectKey.",
  "doc.deployments.trigger.zip.repository": "Deprecated legacy field, still honoured: values starting with http(s) are treated as url, everything else as objectKey.",

  // ── ConfigMaps ────────────────────────────────────────────────────────
  "doc.configmaps.title": "ConfigMap",
  "doc.configmaps.overview.title": "Overview",
  "doc.configmaps.overview.p1": "In every environment, each application has a ConfigMap and a Secret named after it. The endpoints below read and overwrite the key-value pairs of both at once: each entry's `secret` field decides whether it lands in the ConfigMap or the Secret.",
  "doc.configmaps.overview.p2": "Each entry is either an environment variable or a file. **With no `mountPath`**, the key goes into the ConfigMap / Secret named after the application, and the platform injects the whole thing as environment variables through `envFrom` (keys that are not valid environment variable names are silently skipped by Kubernetes). **With a `mountPath` (absolute path)**, the key moves into the companion `{application}.files` ConfigMap / Secret and is mounted as a file via `subPath`, and **no environment variable is injected**. Changing the mount mapping (adding or removing a mount) needs a redeploy for the volume to take effect; changing only values needs just a rolling restart to re-read them.",
  "doc.configmaps.overview.p3": "Every endpoint needs the `environment` query parameter naming the target environment, and the path placeholder is `applicationName` (not the `name` the other controllers use).",
  "doc.configmaps.read.title": "Read the ConfigMap",
  "doc.configmaps.read.summary": "Return every ConfigMap key-value pair of the application in the given environment.",
  "doc.configmaps.read.response.title": "Response",
  "doc.configmaps.read.response.data": "Array of key-value pairs covering every entry in both the ConfigMap and the Secret; see below for the element shape.",
  "doc.configmaps.read.response.key": "Key name — the environment variable name injected into the container.",
  "doc.configmaps.read.response.value": "The value (Secret entries are already decoded to plain text).",
  "doc.configmaps.read.response.secret": "true means the entry comes from the application Secret, false from the ConfigMap.",
  "doc.configmaps.read.response.mountPath": "Optional; the absolute in-container path when the entry is mounted as a file.",
  "doc.configmaps.write.title": "Overwrite the ConfigMap",
  "doc.configmaps.write.summary": "Overwrite the application's ConfigMap in the given environment with the request body.",
  "doc.configmaps.write.body.title": "Request body",
  "doc.configmaps.write.body.p1": "The body is an array, each element describing one key-value pair:",
  "doc.configmaps.write.body.secret": "true writes to the application Secret, false (the default) to the ConfigMap.",
  "doc.configmaps.write.body.mountPath": "Optional absolute path; when set, the entry is additionally mounted as a file.",
  "doc.configmaps.write.notes.title": "Notes",
  "doc.configmaps.write.notes.p1": "This is a full-overwrite endpoint: it rewrites both the ConfigMap and the Secret, and any key missing from the body is deleted from them. For an incremental update, GET the current values, merge, then PUT.",
  "doc.configmaps.write.notes.p2": "After an update, the application Pods need a restart (or a fresh deployment) before the container sees the new environment variables.",

  // ── Sandbox ───────────────────────────────────────────────────────────
  "doc.sandbox.title": "Sandbox",
  "doc.sandbox.overview.title": "Overview",
  "doc.sandbox.overview.p1": "A sandbox runs commands temporarily in an isolated Kubernetes environment. It comes in two shapes:",
  "doc.sandbox.overview.li1": "**One-off execution** (`/executions`): every call creates a K8s Job that is cleaned up once the command finishes.",
  "doc.sandbox.overview.li2": "**Persistent instance** (`/instances`): the sandbox is kept as a StatefulSet, so you can go back in to run commands and browse files.",
  "doc.sandbox.overview.p2": "Every creation endpoint accepts CPU / memory limits and custom environment variables. Environment variable names must match `[A-Za-z_][A-Za-z0-9_]*`.",
  "doc.sandbox.images.title": "List available images",
  "doc.sandbox.images.summary": "Return the sandbox runtime images the platform ships with. Custom images do not have to appear in this list.",
  "doc.sandbox.executions.title": "One-off execution",
  "doc.sandbox.executions.summary": "Run commands once and return the result (or stream it over SSE).",
  "doc.sandbox.executions.body.title": "Request body (SandboxExecutionRequest)",
  "doc.sandbox.executions.body.environment": "Target environment name.",
  "doc.sandbox.executions.body.image": "Runtime image name; either platform-provided or custom.",
  "doc.sandbox.executions.body.commands": "Commands to run in order; each element is a single line of shell.",
  "doc.sandbox.executions.body.timeoutSeconds": "Execution timeout in seconds; the run is killed when it elapses.",
  "doc.sandbox.executions.body.ttl": "Seconds to keep the Job around after it finishes.",
  "doc.sandbox.executions.body.cpu": "CPU resources, shaped { request, limit }, as K8s quantities — for example 100m.",
  "doc.sandbox.executions.body.memory": "Memory resources, shaped { request, limit } — for example 128Mi.",
  "doc.sandbox.executions.body.env": "Environment variables injected into the container; keys and values are strings, for example { \"FOO\": \"bar\" }.",
  "doc.sandbox.executions.body.stream": "When true, returns text/event-stream and pushes stdout/stderr line by line.",
  "doc.sandbox.executions.result.title": "Non-streaming response (SandboxExecutionResult)",
  "doc.sandbox.executions.result.exitCode": "Final exit code of the command.",
  "doc.sandbox.executions.result.logs": "Full output (returned on demand).",
  "doc.sandbox.instances.title": "Persistent instances",
  "doc.sandbox.instances.create.summary": "Create a long-lived sandbox (a StatefulSet).",
  "doc.sandbox.instances.create.body.title": "Request body (SandboxInstanceCreateRequest)",
  "doc.sandbox.instances.create.body.name": "Sandbox name, unique within the environment; generated automatically when omitted.",
  "doc.sandbox.instances.create.body.image": "Runtime image.",
  "doc.sandbox.instances.create.body.cpu": "CPU resources, shaped { request, limit }.",
  "doc.sandbox.instances.create.body.memory": "Memory resources, shaped { request, limit }.",
  "doc.sandbox.instances.create.body.env": "Custom environment variables; keys and values are strings. The built-in alpine-mate image layers defaults such as PUID / PGID / TZ / SUBFOLDER / TITLE underneath yours, and a variable you supply overrides the default of the same name.",
  "doc.sandbox.instances.create.body.keepalive": "Whether to use the default keepalive command (sleep infinity). Defaults to true, which overrides the image's ENTRYPOINT/CMD. Set it to false to keep the image's own entrypoint — useful when the Dockerfile already defines a long-running process.",
  "doc.sandbox.instances.list.summary": "List the sandbox instances visible to the current user.",
  "doc.sandbox.instances.list.params.title": "Query parameters",
  "doc.sandbox.instances.list.params.environment": "Optional; filter by environment.",
  "doc.sandbox.instances.list.params.image": "Optional; filter by image.",
  "doc.sandbox.instances.get.summary": "Fetch the details of one sandbox instance.",
  "doc.sandbox.instances.get.response.title": "Response (SandboxInstance)",
  "doc.sandbox.instances.get.response.status": "One of PENDING / RUNNING / FAILED / TERMINATING.",
  "doc.sandbox.instances.delete.summary": "Destroy a persistent sandbox instance along with its StatefulSet and Service. The response data is null.",
  "doc.sandbox.instances.delete.p1": "Deletion is part of the sandbox lifecycle, so OpenAPI allows `DELETE` on this path; destructive operations such as deleting an application still return 405 (see the `Authentication` chapter).",
  "doc.sandbox.exec.title": "Run a command inside an instance",
  "doc.sandbox.exec.summary": "Run a single command in a sandbox instance that is already ready.",
  "doc.sandbox.exec.body.title": "Request body (SandboxInstanceExecRequest)",
  "doc.sandbox.exec.body.command": "The command to run (a single line of shell).",
  "doc.sandbox.exec.body.timeoutSeconds": "Execution timeout in seconds.",
  "doc.sandbox.exec.body.stream": "When true, returns an SSE stream.",
  "doc.sandbox.terminal.title": "Instance terminal (WebSocket)",
  "doc.sandbox.terminal.summary": "Connect to the sandbox instance's interactive TTY (xterm-256color).",
  "doc.sandbox.terminal.p1": "This is a WebSocket endpoint: the handshake carries the `Authorization: Bearer <access token>` header like every other OpenAPI call (browsers cannot set headers on a WebSocket handshake, so this endpoint targets CLI and machine clients). Once connected, text or binary frames you send are written straight to the remote TTY's stdin, and terminal output comes back as frames.",
  "doc.sandbox.files.title": "Browsing and downloading files",
  "doc.sandbox.files.list.summary": "List the entries of a directory inside the sandbox. An empty path means the root directory.",
  "doc.sandbox.files.list.entry.title": "Response array element (PodFileEntry)",
  "doc.sandbox.files.list.entry.path": "Full path.",
  "doc.sandbox.files.list.entry.type": "One of DIRECTORY / FILE / SYMLINK_DIRECTORY / SYMLINK_FILE / OTHER. Symlinks resolve to SYMLINK_DIRECTORY or SYMLINK_FILE according to what they point at; broken symlinks are OTHER.",
  "doc.sandbox.files.list.entry.size": "Size in bytes; null for directories.",
  "doc.sandbox.files.download.summary": "Download a file from the sandbox. The response is a binary stream, not a Result envelope.",
  "doc.sandbox.files.download.p1": "A single download is capped by `oops.pod-filesystem.max-download-size-bytes`; anything larger returns a business error.",
  "doc.sandbox.content.title": "Reading and saving files",
  "doc.sandbox.content.get.summary": "Read a text file's content (UTF-8). File size is capped by oops.pod-filesystem.max-edit-size-bytes.",
  "doc.sandbox.content.get.response.title": "Response (FileContentResponse)",
  "doc.sandbox.content.get.response.content": "File content (a UTF-8 string).",
  "doc.sandbox.content.put.summary": "Overwrite a text file's content. The parent directory must already exist.",
  "doc.sandbox.content.put.body.title": "Request body (FileSaveRequest)",
  "doc.sandbox.content.put.body.path": "Absolute path of the target file.",
  "doc.sandbox.content.put.body.content": "The new file content.",
  "doc.sandbox.upload.title": "Uploading files",
  "doc.sandbox.upload.summary": "Upload a file into a directory in the sandbox as multipart/form-data. The file form field is required.",
  "doc.sandbox.upload.params.title": "Query parameters",
  "doc.sandbox.upload.params.path": "Absolute path of the target parent directory; the file name is appended automatically.",
  "doc.sandbox.upload.p1": "A single upload is capped by `oops.pod-filesystem.max-upload-size-bytes` (50MB by default) and also bound by Spring's `spring.servlet.multipart.max-file-size`. File names are validated: `.`, `..` and newline characters are rejected.",
  "doc.sandbox.mutate.title": "Deleting, renaming and creating directories",
  "doc.sandbox.mutate.delete.summary": "Delete a file or directory inside the sandbox (recursively). The response data is null.",
  "doc.sandbox.mutate.delete.p1": "Deleting sandbox files is a sandbox instance lifecycle operation, so it stays open under `/openapi/sandbox/**` (see `Authentication`).",
  "doc.sandbox.mutate.rename.summary": "Move a file or directory inside the sandbox from fromPath to toPath. Both ends must be on the same filesystem.",
  "doc.sandbox.mutate.rename.body.title": "Request body (FileRenameRequest)",
  "doc.sandbox.mutate.rename.body.fromPath": "Source path; must exist.",
  "doc.sandbox.mutate.rename.body.toPath": "Target path; must not exist.",
  "doc.sandbox.mutate.mkdir.summary": "Create a directory inside the sandbox (equivalent to mkdir -p). Fails when the path already exists, including a symlink of the same name.",
  "doc.sandbox.mutate.mkdir.body.title": "Request body (DirectoryCreateRequest)",
  "doc.sandbox.mutate.mkdir.body.path": "Absolute path of the directory to create; cannot be the root /.",
}

export default doc
